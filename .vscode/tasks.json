{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "taskName": "build stream",
            "type": "shell",
            "command": "g++",
            "args": [
                "-g -std=c++14 -W -Wall -Wpedantic -I ./include main.cpp"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}